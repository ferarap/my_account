require "spec_helper"

describe Movement do
  it "should be valid" do
    #movement = Movement.new(date: Time.now, subject: "new movement", amount: "30", type_movement: "debe")
    #movement.should be valid
    pending "implement test of valid movement"
  end
end